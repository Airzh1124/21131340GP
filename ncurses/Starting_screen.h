#include <string>
#include <vector>
#include <iostream>
#include <ncurses.h>
using namespace std;

extern const vector<string> ship;

void Print_map(const vector<string>& art);

//Function for printing out the ASCII art in the starting screen
